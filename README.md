# Agentic-AI
create a .env file in the project folder. Place the GROK_API_KEY=" " and EXA_API_KEY=" " from groq and exa respectively.
pip install groq
pip install exa
The above installations are required.
